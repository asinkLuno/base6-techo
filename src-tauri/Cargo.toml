[package]
name = "base6-techo"
version = "0.1.0"
description = "Printable notebook desktop application"
authors = ["guozr"]
edition = "2024"

# tauri dev 的 cargo run 在多 bin 时需要指定默认二进制（另一个是 techo-pipeline CLI）。
default-run = "base6-techo"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "base6_techo_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
# the same as the package name
name = "base6-techo"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde", "unstable-locales"] }
chrono-tz = "0.10"
chinese-lunisolar-calendar = "0.2.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
